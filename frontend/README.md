# Frontend (Vue 3 + Vite + Element Plus)

## Run locally

```bash
cd /Users/guyin/Desktop/Echooo/confyUI-flow-back/frontend
cp .env.example .env
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Login

- Username: `admin`
- Password: `admin666`

Credentials are configured in backend `.env`:

- `ADMIN_USERNAME`
- `ADMIN_PASSWORD`
